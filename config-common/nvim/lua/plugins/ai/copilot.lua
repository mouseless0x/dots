return {
	--"zbirenbaum/copilot.lua",
	--event = "InsertEnter", -- load only when you start typing
	--cmd = "Copilot",
	--config = function()
	--	require("copilot").setup({
	--		suggestion = {
	--			enabled = true,
	--			auto_trigger = true, -- show as you type
	--			hide_during_completion = false, -- default: hides when a completion menu is open
	--			keymap = {
	--				accept = "<Tab>",
	--			},
	--		},
	--	})
	--end,
}
